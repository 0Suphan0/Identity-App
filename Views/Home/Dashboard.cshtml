@{
    ViewData["Title"] = "Dashboard";
}

<div class="row justify-content-center">
    <div class="col-lg-10">

        <div class="d-flex align-items-center justify-content-between mb-3">
            <div>
                <h2 class="mb-1">Dashboard (Protected)</h2>
                <div class="text-muted">
                    You are authenticated via <strong>Identity Cookie</strong>.
                </div>
            </div>
            <span class="badge text-bg-success">Authorized</span>
        </div>

        <div class="card mb-4">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-4">
                        <div class="text-muted small">User.Identity.Name</div>
                        <div class="fw-semibold">@User.Identity?.Name</div>
                    </div>
                    <div class="col-md-4">
                        <div class="text-muted small">Authentication Type</div>
                        <div class="fw-semibold">@User.Identity?.AuthenticationType</div>
                    </div>
                    <div class="col-md-4">
                        <div class="text-muted small">IsAuthenticated</div>
                        <div class="fw-semibold">@User.Identity?.IsAuthenticated</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <strong>Claims in this request</strong>
                <span class="text-muted small ms-2">(Cookie → ClaimsPrincipal)</span>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped mb-0">
                        <thead>
                            <tr>
                                <th style="width: 35%">Type</th>
                                <th>Value</th>
                                <th style="width: 20%">Issuer</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var c in User.Claims)
                            {
                                <tr>
                                    <td class="text-break">@c.Type</td>
                                    <td class="text-break">@c.Value</td>
                                    <td class="text-break">@c.Issuer</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div class="mt-3 text-muted small">
            Bu tablo, tarayıcıdaki <strong>Identity Cookie</strong> okunarak oluşturulan kullanıcı kimliğini (claims) gösterir.
        </div>

    </div>
</div>
